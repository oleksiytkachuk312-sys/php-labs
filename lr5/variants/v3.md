# Варіант 3 — Лабораторна робота №5

> Варіант: 3
> Лаба: LR5 — Файли та база даних (PDO)
> Тема сайту: Кінотеатр

## Завдання 1: Робота з файлами

### 1.1 Гостьова книга

- Форма: **Ім'я** + **Коментар** → збереження у файл `data/comments.txt`
- Формат: pipe-розділювач (`дата|ім'я|коментар`)
- Виведення всіх коментарів з файлу в таблицю (з датою)

### 1.2 Завантаження зображень

- Форма upload зображень (JPEG, PNG, GIF, WebP, до 5 МБ)
- Збереження в `data/uploads/`
- Галерея всіх завантажених зображень

### 1.3 Робота з каталогами

- Форма: **Логін** + **Пароль** → створити папку `data/users/{логін}/` з підпапками `video`, `music`, `photo` + по 2-3 файли
- Якщо папка існує → помилка
- Окрема сторінка видалення: **Логін** + **Пароль** → видалити папку з усім вмістом

## Завдання 2: База даних (PDO)

### 2.1 CRUD — Фільми

Таблиця `movies`:

| Поле | Тип | Опис |
|------|-----|------|
| id | INT (PK, AUTO_INCREMENT) | Унікальний ідентифікатор |
| title | VARCHAR(200) | Назва фільму |
| director | VARCHAR(100) | Режисер |
| genre | VARCHAR(50) | Жанр |
| year | YEAR | Рік виходу |
| duration_min | INT | Тривалість (хвилин) |
| created_at | DATETIME | Дата створення |

**Сторінки:**
- Список всіх записів у таблиці
- Додавання нового запису (форма з валідацією)
- Редагування існуючого запису (форма з передзаповненням)
- Видалення запису за ID

**Вимоги:** prepared statements (`prepare` + `execute`), `try/catch` для PDO

### 2.2 Авторизація через БД

Таблиця `users` (10 полів):

| Поле | Тип |
|------|-----|
| id | INT (PK) |
| login | VARCHAR(50) UNIQUE |
| password | VARCHAR(255) |
| email | VARCHAR(100) |
| first_name | VARCHAR(50) |
| last_name | VARCHAR(50) |
| phone | VARCHAR(20) |
| city | VARCHAR(50) |
| gender | VARCHAR(10) |
| about | TEXT |
| created_at | DATETIME |

**Правила:**
- Унікальний логін
- Пароль: мінімум 6 символів
- E-mail формат
- Хешування: `password_hash` / `password_verify`

**Сторінки:** реєстрація, вхід, профіль, редагування, видалення акаунту, вихід

### 2.3 Сесії та Cookie (з ЛР4)

- Колір фону → `$_SESSION['bg_color']` (5+ кольорів за темою)
- Привітання → cookie на 30 днів

**Кольори за темою (5):** Нічний синій (#191970), Оксамитовий бордо (#800020), Срібний екран (#C0C0C0), Золото Оскара (#FFD700), Темний антрацит (#2F2F2F)

## Примітки

- Дивись `demo/` для прикладу реалізації
- Повністю розв'язаний варіант: `variants/v30/`
- Структура файлів має відповідати demo

## Корисні посилання

- [PHP File Handling](https://www.w3schools.com/php/php_file.asp) — робота з файлами
- [PHP File Upload](https://www.w3schools.com/php/php_file_upload.asp) — завантаження файлів
- [PHP MySQL (PDO)](https://www.w3schools.com/php/php_mysql_intro.asp) — бази даних
- [PHP Prepared Statements](https://www.w3schools.com/php/php_mysql_prepared_statements.asp) — prepared statements
- [PHP password_hash](https://www.php.net/manual/en/function.password-hash.php) — хешування паролів
- [PHP Sessions](https://www.w3schools.com/php/php_sessions.asp) — сесії
- [PHP Cookies](https://www.w3schools.com/php/php_cookies.asp) — cookies
